<h2>Fotos Feed</h2>

<% if @fotos.empty? %>
  No Fotos

<% else %>
  <div>
    <% @fotos.each do |foto| %>
      <div>
	    <%= link_to (image_tag foto.image.url(:medium), class: 'foto'), foto %>
	      </div>
    <% end %>
  </div>
<% end %>
